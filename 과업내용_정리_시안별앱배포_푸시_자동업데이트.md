# 시안별 앱 배포 · 푸시 · 자동 업데이트 과업내용 정리

작성일 2026-07-30 / 지역균형관광팀
대상 시안 : 디지털 관광주민증 · 반값여행 홈페이지 시안 **A~F**

---

## 1. 과업 요지

홈페이지로 만든 시안 A~F를 **시안별 개별 앱으로 설치·비교**할 수 있게 하고, **시안별 알림 발송**과 **접속 시 자동 업데이트**를 붙였다. 세 가지 모두 구현·검증 완료했으며, 실시간 푸시(FCM)만 외부 계정이 필요해 미적용 상태다.

| 과업 | 상태 | 산출물 |
|---|---|---|
| ① 시안별 앱 다운로드 | **완료** | APK 6종 + 다운로드 페이지 |
| ② 시안별 앱 푸시 + 관리자 발송 기능 | **완료**(확인 방식) / FCM 전환 미적용 | 앱 알림 모듈 + 관리자 화면 2곳 |
| ③ 접속 시 UI/UX·기능 자동 업데이트 | **완료** | version.json 기반 갱신 처리 |

---

## 2. 산출물 위치

```
code/dtid/
├─ android-siteapp/                     ← [신규] 시안 앱 셸 (Android)
│  ├─ app/build.gradle                    시안 A~F를 flavor로 정의
│  └─ app/src/main/java/kr/knto/dtid/siteapp/
│     ├─ MainActivity.java                WebView 셸 · 서버주소 설정 · 갱신 안내
│     ├─ UpdateChecker.java               version.json 확인 → 자동/선택/필수 갱신 판정
│     ├─ Notices.java                     notices.json 확인 → 시안별 알림 표시
│     ├─ NoticeJobService.java            15분 주기 알림 확인
│     ├─ BootReceiver.java                재부팅 후 주기 재등록
│     ├─ Prefs.java                       서버주소·최근 처리시각 보관
│     └─ Http.java                        JSON 조회(외부 라이브러리 없음)
│
└─ dgtour/                               ← 기존 시안 홈페이지
   ├─ download.html                        [신규] 시안별 앱 다운로드(QR 포함)
   ├─ admin-push.html                      [신규] 앱 푸시 관리(발송·대상·이력)
   ├─ version.json                         [신규] 자동 업데이트 설정
   ├─ notices.json                         [신규] 발송 건 목록
   ├─ serve.py                             [신규] 사내망 배포용 정적 서버
   ├─ apk/dtid-site{a~f}.apk               [신규] 시안별 앱 6종
   ├─ sites.html                           [수정] 앱 다운로드·푸시 진입 링크 추가
   ├─ site-f/admin.html                    [수정] 「앱 푸시」 관리자 탭 신설
   └─ site-{a~e}/t50-admin.html            [수정] 앱 푸시 관리 진입 링크 추가
```

원본 시안 페이지(`site-a/`~`site-f/` 화면 자체, `assets/`, `site-assets/`)는 수정하지 않았다.

---

## 3. 과업별 내용

### ① 시안별 앱 다운로드

시안 A~F를 Android **flavor**로 나눠 각각 별도 APK로 빌드했다.

| 시안 | 앱 이름 | 패키지 | 파일 |
|---|---|---|---|
| A | 시안A 월렛퍼스트 | `kr.knto.dtid.siteapp.a` | `dtid-sitea.apk` |
| B | 시안B 여행탐색 | `kr.knto.dtid.siteapp.b` | `dtid-siteb.apk` |
| C | 시안C 미션 | `kr.knto.dtid.siteapp.c` | `dtid-sitec.apk` |
| D | 시안D 공공미니멀 | `kr.knto.dtid.siteapp.d` | `dtid-sited.apk` |
| E | 시안E 통합안 | `kr.knto.dtid.siteapp.e` | `dtid-sitee.apk` |
| F | 시안F 컨설팅안 | `kr.knto.dtid.siteapp.f` | `dtid-sitef.apk` |

- 패키지명이 서로 달라 **한 대의 휴대폰에 6개를 동시 설치**해 나란히 비교 가능
- 앱 이름·아이콘에 시안 구분이 드러나 홈 화면에서 바로 식별
- `download.html` 에서 시안별 카드 · APK 직접 다운로드 · **QR 코드**(외부 라이브러리 없이 자체 생성) 제공
- 디버그 키로 서명해 별도 절차 없이 설치 가능(스토어 배포 시 릴리즈 키 교체 필요)

### ② 시안별 앱 푸시 및 관리자 발송 기능

**앱 측** — `notices.json` 을 읽어 자기 시안이 대상인 미수신 건만 골라 알림으로 띄운다.
- 대상 지정은 `"all"` 또는 시안 코드 배열(`["a","c"]`) 모두 허용
- 최근 처리시각 이후 발송분만 표시해 중복 알림 방지
- 알림을 누르면 지정 화면으로 이동(`link` 항목)
- 확인 시점 : **앱 접속 시 + 약 15분 주기**(재부팅 후에도 유지)

**관리자 측** — 두 경로로 제공한다.
- `admin-push.html` (전용 화면) : 대상 시안 선택, 제목·내용·이동화면·발송시각 입력, 알림 미리보기, 테스트 발송, 발송 목록·예약 표시, `notices.json` 생성·내려받기
- `site-f/admin.html` 「앱 푸시」 탭 : 관리자 대시보드 안에서 대상 선택 후 발송 건 생성
- 안전장치 : 전체 발송 2단계 확인, 동일 제목·내용 중복 경고, 개인정보 미포함 안내

### ③ 접속 시 UI/UX·기능 자동 업데이트

앱이 시안 홈페이지를 서버에서 읽어오는 구조이므로 **홈페이지를 수정하면 앱에 그대로 반영**된다. 여기에 `version.json` 기반 판정을 더했다.

| 구분 | 조건 | 앱 동작 |
|---|---|---|
| 화면·기능 갱신 | `webVersion` 값 변경 | 캐시 비우고 재로딩 → 즉시 반영, 이용자 조작 없음 |
| 선택 갱신 | 앱 버전 < `latestVersionCode` | 안내 후 ‘나중에’ 선택 가능 |
| 필수 갱신 | 앱 버전 < `minVersionCode` | 갱신 전까지 이용 제한 |

- 서버에 닿지 않거나 JSON 형식이 깨져도 **기존 상태로 정상 구동**(앱 중단 없음)
- 검토자 PC의 IP가 매번 바뀌므로 **앱에서 서버 주소 직접 변경** 가능(오류 문구 탭 또는 뒤로가기 길게 누르기)

---

## 4. 사용 방법

```
1) 시안 서버 실행 (PC)
   cd code/dtid/dgtour
   python serve.py                    → 표시되는 http://IP:8080/ 주소 확인

2) 앱 설치 (휴대폰, 같은 사내망)
   http://IP:8080/download.html 접속 → 시안별 [앱 내려받기] 또는 QR
   ※ '출처를 알 수 없는 앱 설치' 허용 필요
   ※ 앱이 서버를 못 찾으면 안내 문구를 눌러 위 주소 입력

3) 알림 발송 (관리자)
   http://IP:8080/admin-push.html → 대상 시안·내용 입력 → [발송 목록에 추가]
   → [notices.json 내려받기] → dgtour/notices.json 덮어쓰기

4) 화면 수정 반영
   site-x/ 페이지 수정 → version.json 의 webVersion 값 변경
   → 앱 재접속 시 자동 반영
```

### 앱 재빌드

```
cd code/dtid/android-siteapp
JAVA_HOME=C:/cowork/android-dev/jdk17 \
ANDROID_HOME=C:/cowork/android-dev/sdk \
C:/cowork/android-dev/gradle/bin/gradle --offline assembleRelease
→ app/build/outputs/apk/site{a~f}/release/ 에 생성 후 dgtour/apk/ 로 복사
```

기본 서버 주소를 빌드에 넣으려면 `-PbaseUrl=http://192.168.0.10:8080/` 를 붙인다.

---

## 5. 검증 결과

| 항목 | 결과 |
|---|---|
| APK 6종 빌드 | 성공 (오프라인 빌드, 각 약 17.9KB) |
| 패키지명·앱 이름 분리 | 6종 모두 상이 확인 → 동시 설치 가능 |
| 권한 | INTERNET · POST_NOTIFICATIONS 정상 포함 |
| APK 배포 | HTTP 200, MIME `application/vnd.android.package-archive` |
| 시안 시작 페이지 | A~F 6종 모두 HTTP 200 |
| 자동 업데이트 판정 | webVersion 변경 시 재로딩 / latest·min 조건별 선택·필수 갱신 정상 |
| 시안별 알림 대상 | `all` → 6종 전체, `["a","c"]` → A·C만 수신 확인 |
| 중복 알림 방지 | 최근 처리시각 이전 건 제외 확인 |

---

## 6. 확인·결정이 필요한 사항

### 실시간 푸시(FCM) 전환 — 외부 계정 필요
현재는 Firebase 없이 동작하도록 **앱이 확인하는 방식**(접속 시 + 15분 주기)으로 만들었다. 시안 검토용으로는 충분하나, 서버가 즉시 밀어주는 진짜 푸시는 아니다. 전환에 필요한 것:
1. Firebase 프로젝트 개설 및 `google-services.json` 발급
2. 앱에 FCM 모듈 추가(시안별 토픽 구독)
3. 관리자 발송을 FCM 전송으로 교체

대상 구분(시안별)·발송 이력·관리자 화면 구조는 그대로 재사용되므로, 추가 작업은 전송 계층 교체에 한정된다.

### iOS 미지원
서명·심사 절차가 필요해 이번 범위에서 제외했다. 제공하려면 Apple Developer 계정과 TestFlight 등 내부 배포 채널 준비가 선행된다.

### 관리자 화면의 저장 방식
시안이 정적 홈페이지라 발송 내용을 서버에 직접 저장할 수 없어, `notices.json` 을 내려받아 덮어쓰는 방식으로 만들었다. 운영 시스템 반영 시 **입력·대상·이력 UI는 그대로 두고 저장만 서버 API로 교체**하면 된다.

### 배포 범위
내부 검토용 빌드로 스토어에 등록되어 있지 않다. 사내망(같은 공유기) 접속을 전제하며, 외부 검토자에게 배포하려면 외부 접속 가능한 서버 배치와 접근 인증이 추가로 필요하다.

---

## 7. 후속 과업 (참고)

- 시안 확정 후 선정안 기준 단일 앱으로 통합
- 실서비스 전환 시 : FCM 적용, 릴리즈 키 서명, 스토어 등록, 접근 인증
- 디주는 현재 대한민국 구석구석 플랫폼 내 서비스이므로, 전용 앱 방향은 대구석 담당 부서와 협의 필요
- 앱 배포·푸시 체계의 SW개발비(기능점수) 산정 별도 검토
